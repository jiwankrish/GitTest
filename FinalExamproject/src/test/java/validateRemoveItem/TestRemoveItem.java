package validateRemoveItem;

public class TestRemoveItem {
	// Test 2: Validate that a single list item could be removed using the remove
	// button when a single checkbox is selected.
	/*
	 * WebDriver driver;
	 * 
	 * @Before public void startUp() { driver = BrowserFactory.openBrowser(); }
	 * 
	 * @Test public void testRemove() { TestPage testPage =
	 * PageFactory.initElements(driver, TestPage.class); String intialText =
	 * testPage.getTextItem1(); testPage.clickItem1(); testPage.clickRemove();
	 * Assert.assertNotEquals(intialText, testPage.getTextItem1()); }
	 */

}
